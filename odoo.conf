[options]
# Database settings (will be set via environment variables)
# These will be set via Railway environment variables
db_host = ep-delicate-wildflower-a5wd2gio.us-east-2.aws.neon.tech
db_port = 5432
db_user = neondb_owner
db_password = npg_cUsR6jI2mXHo
db_name = neondb
db_sslmode = require

# Server settings
# http_port will be set via ODOO_HTTP_PORT environment variable
longpolling_port = 8072
workers = 2
max_cron_threads = 1
limit_memory_hard = 2147483648
limit_memory_soft = 1073741824
limit_request = 8192
limit_time_cpu = 60
limit_time_real = 120

# Paths
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /app/filestore

# Security
admin_passwd = admin
list_db = False
proxy_mode = True

# Logging
log_level = info
log_handler = :INFO

# Performance
osv_memory_age_limit = 1.0
osv_memory_count_limit = 0

# Without demo data
without_demo = all

# Additional settings
http_interface = 0.0.0.0
syslog = False
log_db = False
max_file_upload_size = 100
session_dir = /tmp/sessions 